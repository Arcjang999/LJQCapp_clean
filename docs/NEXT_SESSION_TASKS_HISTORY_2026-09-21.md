> 历史归档：2026-09-21 换 session 前的原文完整保留。此处的“最新”“下一步”“未提交”等只代表各段写作时的状态；当前接手请读 [开发交接](DEVELOPMENT_HANDOFF.md) 和 [下一阶段任务](NEXT_SESSION_TASKS.md)。不要按本归档启动旧分支或重复开发。

# LJQC 下一阶段任务书与 session 交接

## 当前优先顺序（2026-09-21）

用户已授权提交推送本阶段代码、测试和文档；交付到 `origin/codex/project-workflow-alignment`（`Arcjang999/LJQCapp_clean`），实际提交号接手时核查。下方旧“保持未提交”“从试剂旧表单开始”属于过程记录。

1. 按 `AGENTS.md` 阅读交接，再读 [新版使用说明](current_user_guide.md) 及各步实施说明。首页使用指南已同步，项目/批次/基础资料/换批/参数/历史弹窗已接通，不重复开发。
2. **先执行 [分子、免疫等标准扩展与适用标准必选](task_00_standards_expansion.md)**：核查 403 已有免疫覆盖，补充分子、定性免疫等官方标准，逐条处理生效版本、范围和指标类型。项目存在现行适用标准时必须采用；无适用条目才登记实验室要求。当前仅规划此扩展，运行目录仍为 94 条，不把说明更新当功能完成。
3. 用同一配置链完成标准必选、混合项目、实际材料、三方法录入、换批/参数、追溯与报告的整条验收；再补特定高缩放及打包运行验收，核心计算与旧数据不变。
4. 继续原任务 1→2→3：失控处理与独立报告、今日总览/多项目录入、换批比对/批量月报。均保留原任务书，不因界面统一视为完成。

质量标准选用、来源登记和自动评价分开：过程/定性条款不是 CV；Ct/log 不套用浓度 CV；参考区间不是分析质量要求。明确适用时不得用随意填写“不适用”绕过，具体实现及验收见新任务书。

## 历史开发记录

以下保留各阶段完成时的说明；当前范围、开发顺序及交付分支以文首为准。

2026-09-21 最新接续：上一段安排的试剂换批与历史事件更正交互已完成，先读 [试剂与历史弹窗实施说明](reagent_dialog_workflow_implementation.md)。23 套相关隔离测试及实际浏览器验收通过；正常填写误报资料变更、内置检验项目本地备注覆盖问题已修复，核心计算及原用户库未改。下一步转入整条业务流程、高缩放和打包验收，不再重做已统一的弹窗。原任务 1→2→3 仍未开发。保留全部未提交修改；HEAD 为 `f263cff`，未推送。下文较早同日接续为历史记录。

2026-09-20 最新接续：上一段安排的质控品换批及均值和标准差交互现已完成，见 [换批与参数弹窗实施说明](lot_lifecycle_dialog_workflow_implementation.md)。21 套相关隔离测试和实际浏览器验收通过，保存后定位新批次/参数版本，历史数据及核心计算保持原规则。下一步处理“试剂批号与换批”和“历史记录”中试剂事件更正的旧表单，再做全流程、高缩放和打包验收；原任务 1→2→3 仍未开发。HEAD 仍为 `f263cff`，未推送，保留本步及此前全部未提交修改。下文相同日期的接续段落为之前各步记录。

2026-09-20 最新接续：基础资料列表、详情、新增/编辑弹窗及停用/恢复确认已完成，见 [基础资料弹窗实施说明](master_data_dialog_workflow_implementation.md)。17 套相关隔离测试及浏览器操作通过，不再把其他基础字典列为待改。下一步从“更换质控品批次”及“批号使用与追溯 → 均值和标准差管理”交互继续，保留现有换批/参数/质量来源规则，再做全流程、高缩放和打包验收；原任务 1→2→3 仍未开始。HEAD 为 `f263cff`（未推送），保留前一步批次和本步全部未提交修改。

2026-09-20 最新接续：已按用户要求先提交检查点 `f263cff`（未推送），再完成批次列表、检验项目只读详情、新增批次/水平设置/质量目标弹窗，当前后续改动未提交。先读 [批次弹窗实施说明](batch_dialog_workflow_implementation.md)，保留工作区，按反馈继续其他基础字典和换批页面交互，不重做已验收的参数与来源链路。原任务 1→2→3 尚未开发。

2026-09-20 最新试用决定：项目停用放在编辑项目中，并须两次确认；质控方法使用“单水平（LJ）/多水平法”，即时法作为单水平内选择。当前首版与此前文档先作本地检查点提交，再继续批次列表/详情与独立编辑弹窗，沿用现有参数保存、质量目标确认和批次启用校验。原任务 1→2→3 仍待后续，不因本次改界面视为完成。

2026-09-20 用语要求：用户试用后要求严格参考两份说明书，不自创语言。新增界面已做一轮文案修正；下次开发先读 [界面用语与手册对照](ui_wording_manual_alignment.md)，沿用“方法学、浓度水平、批号、质量目标、均值和标准差”等业务用语，不再把“兼容、绑定、冻结、候选处置”等开发词放进页面。计算、强制确认和数据保护规则保留。

## 最新接续：先试用项目中心首版，再收敛余下界面（2026-09-20）

首版已实施，范围和证据见 [实施与验收](ui_workflow_trial_implementation.md)。项目入口、项目/检验项目/材料弹窗、浓度与实际批号登记、混合方式路由、适用标准或实验室来源确认，以及批次状态确认均已接通。29 套相关回归通过，原用户库未改动，未提交推送。此前全部未提交资料保留。

下一 session 先按 `AGENTS.md` 阅读，再读实施说明及 [改造计划](workflow_ui_refactor_plan.md)。先基于用户试用反馈改进首版，不重新从阶段 0 开始；继续统一详细批次参数和其他基础字典交互，补高缩放与打包验收，再接原任务 1→2→3。项目入口不等于今日总览、多项目录入或失控处理已完成。

质量来源服务准入已实现，不能继续按下文“仅计划、允许空来源”理解现状：旧已固定配置继续使用，新/编辑草稿需确认，有标准候选必须采用或逐项记录不适用依据；Ct/log 保留有来源的实验室要求，不套用浓度 CV。副本迁移、实际材料快照、换批及转换已回归。8504 是独立演示库，接手前重查服务状态，勿当作真实用户数据。

## 最新开发顺序：基线 → 弹窗交互样板 → 全流程（2026-09-20）

用户最新要求是先写开发计划，核心计算不更改，新增/编辑使用弹窗，删除/停用须弹窗确认。完整方案见 [项目中心流程与弹窗交互改造计划](workflow_ui_refactor_plan.md)，本段优先于下文历史顺序。

1. 按 `AGENTS.md` 阅读交接资料，再读上述计划；保留 `codex/project-workflow-alignment` 的全部未提交代码和文档。
2. 先做阶段 0：区分已验收修改与流程初稿，保留可复现基线，建立算法与材料/参数对应关系的回归样例。
3. 再做阶段 1：在独立库实现真实 Streamlit 列表、详情、新增/编辑弹窗、停用确认和返回状态样板，供用户实际体验；不以静态效果图替代交互验证。
4. 样板通过后按阶段 2→5 完成产品材料关系、项目/批次设置、项目中心工作台及追溯/迁移/报告验收。不要沿用展开式表单初稿直接扩写；算法、3/20 点转换及历史冻结规则保持不变。
5. 原任务 1→2→3 继续保留，共同流程与交互基础完成后接续；此次项目入口改造不代表今日总览、多项目录入已完成。

本轮只整理计划，未执行上述开发或未来验收，未迁移真实数据库、未提交推送。初稿已有通过与未通过的检查见下文及最新交接，不能套用早期质量目标的验收结论。

2026-09-20 补充：用户要求有适用标准的检验项目必须关联标准。已写入最新计划第 5.5 节：具体检验项目选择时带出候选并确认，批次逐水平确认适用条件；没有适用条目时记录自定要求和依据。内置 WS/T 来源准确标为卫生行业标准；不能仅按名称或“分子类”判断适用，不能以 Ct/log 硬套浓度尺度要求。当前只是计划，原服务仍允许空目标，统一服务校验及跨入口验收待开发。

## 当前分支与进度（2026-09-20）

现已按用户要求切换到 `codex/project-workflow-alignment`，保留全部未提交代码和文档。用户已确认默认带入、逐项调整；工作分组与质控方式独立，首页向项目中心导航调整。流程改造初稿正在开发，尚未完成端到端验收；下一步与已运行/未完成的验证详见 [最新交接](DEVELOPMENT_HANDOFF.md)。不要把本次建分支视为功能完成或提交推送授权。

## 当前插单：产品浓度规格、项目分类及批次整条链（2026-09-20）

用户要求将浓度水平和浓度编号分开，并一并核查新建项目中单水平/多水平与方法学分类及前后流程。最初核查发现首次多水平配置受单批号限制、产品水平顺序与组合位置耦合等问题；原始事实、目标流程与验收场景见 [整条流程核查](project_material_workflow_alignment.md)。目前已有未验收初稿；项目默认带入、检验项目逐项调整已获用户确认，不能仍视为待选。实施安排以文首最新计划为准，不能把此前质量目标入口调整当作本项完成。此前未提交代码与文档全部保留。

## 2026-09-20 补充：质量目标确认入口已调整

用户反馈“项目默认要求 / 批次采用要求”难懂，已统一为质量目标页的“质量目标设置”；各水平确认直接放在“批次管理”的相应检验项目中，已有批次显示“本批次质量目标”且只读。此项属于任务 2 的局部设置衔接，今日总览、多项目录入及其它设置引导仍未实现。下一接手仍从任务 1 开始，保留此前未提交文档与本次代码/文档修改。使用步骤见 [质量目标实施说明](quality_targets_implementation.md)。

## 最新接手指令：三阶段任务书（2026-09-18）

本段优先于下文历史安排。用户要求把体验差距中的 1、2、3 三步写为任务书，准备在新 session 继续；本轮只更新文档，三阶段功能尚未实现。

| 顺序 | 任务书 | 接手重点 |
|---|---|---|
| 1 | [失控原因分析、复测关联、附件与独立报告](task_01_out_of_control_reports.md) | 从原检测进入处理，保留原判读；完整打通记录、复测、附件备份和报告历史 |
| 2 | [今日质控总览、多项目录入与设置衔接](task_02_daily_workbench.md) | 展示今日结果及未完成处理；减少多项目切换与设置跳转，复用三方法保存逻辑 |
| 3 | [换批比对与批量月报](task_03_comparisons_and_monthly_reports.md) | 按 3A 质控品新旧批号、3B 试剂换批配对、3C 批量月报分别交付 |

当前功能基线为 `72cc9d1 feat: add versioned quality targets and align QC workflows`，已于本会话推送 `origin/codex/v1-2-lj-integration`。术语整理、换批流程核查及质量目标首轮均完成，不再从第 2.1 节重做；下一 session 从任务 1 开始。9月18日本次新增任务文档尚未提交/推送，保留工作区修改。

本轮只读检查本机 `data/qc_lj_app.db`：尚无 `qc_quality_catalog`；仍有 3 次 Z-score 检测、6 条水平结果。原库未运行质量目标迁移，首次以新代码启动时会自动增量迁移；不能把副本验收写成原库已迁移。测试继续用独立数据库，不把种子写入用户库。8501/8502/8503 的实际进程和数据来源在新 session 重查，不依据旧服务描述操作。

参考资料：昆涞使用本地 Q-expert V1.464 手册；QCBOX 暂按伯乐产品及[公开实施研究](https://formative.jmir.org/2025/1/e77043)理解，尚无对应版本手册，不能把 iLab 联合实现的能力全部归为 QCBOX，也不能用 Unity 手册替代其版本证明。操作参考不自动扩展本地单机产品范围。

## 2026-09-18 提交同步说明

用户已授权将当前完成的代码、测试及保留的交接文档一并提交推送到 `codex/v1-2-lj-integration`。下文未提交状态及旧 HEAD 均为历史记录，后续接手以实际 Git 记录为准。数据库、备份、参考手册 PDF 和本地验收输出不随源码提交。下一接手点仍为第 4 节失控原因分析与处理、失控报告。

## 2026-09-17 质量目标完成与下一接手点

用户确认继续后，第 4 节“质量目标”首轮已完成：94 条内置来源要求、查询与自定义 CSV 导入、项目默认版本、新批次逐水平采用确认、旧批次冻结、工作台/月报 CV 比较。复制批次和部分水平换批需重新确认适用条件；即时法转入 LJ 保留原采用要求。SD、偏倚、TEa 只展示，尚未自动评价。来源与完整范围见 [质量目标实施与验收](quality_targets_implementation.md)。

18 个相关套件通过；最后补齐后重跑质量目标（9 组）、换批边界和 CV 设置；完成实际浏览器、12 页月报及用户库副本迁移检查。原用户库未修改，下次正式启动会自动执行 `quality_targets_2024_001` 增量迁移。8503 为本轮隔离预览，使用 `output/quality-targets-2026-09-17/qa.db`；接手时重查服务状态。原有未提交文档及改动全部保留，HEAD 仍为 `cbf3513`，未提交/推送。

下一接手点建议为第 4 节“失控原因分析与处理、失控报告”；附件随报告建设。其他扩展及独立分析批/暂定常用参数等缺口仍待实现。下文旧状态保留为历史，其中“质量目标尚未开始”以本段为准。

## 2026-09-16 执行结果与下一接手点

第 2.1 节应用术语整理和第 2.2 节换批核查已完成本轮交付，具体实现、代码与 WS/T 641 条款对照、16 个通过的测试套件及浏览器/PDF 验收见 [实施与验收说明](terminology_workflow_implementation.md)。已加已有新批的核对入口，保留新批数据衔接与旧批停用保护；没有改变统计算法、即时法 3/20 点规则或迁移用户库。

下一接手点为第 4 节，建议先做质量目标。独立分析批计数、暂定与常用参数、短稳定性质控品方案及专门新旧批比对报告仍未实现，不能因本轮改名标记为完成。当前所有改动未提交/推送，接手前继续保留；8502 为本轮隔离预览，8501 未启动，实际监听状态下次重查。

下文原任务要求和 9月15日交接内容保留作验收依据与历史记录；“尚未修改应用”等旧状态以本段为准。

更新时间：2026-09-15。用户准备在同一台 Mac 的新 session 继续开发。

2026-09-15 已确认交接：用户认可上一轮解释，要求按此更新 README 和任务书。已整理[术语与换批业务口径](terminology_and_workflow_alignment.md)。下一 session 首先规范名称，再核查换批过程，之后按第 4 节分阶段推进选定的五项扩展。当前仅完成术语、标准核对和需求文档，尚未修改应用、合并入口或实现五项扩展。

当前提交：`cbf3513 feat: streamline QC setup and batch replacement workflows`，9月11日功能已提交并推送。9月15日检查本地 HEAD 与本地记录的远端分支为 0/0；本次未重新联网核对远端。README、DEVELOPMENT_HANDOFF、NEXT_SESSION_TASKS 和新增术语文档是本地未提交修改，需保留。数据库、备份和测试输出仅保存在本机。

第 3 节为9月11日开发与验收历史，其中“未提交”“8501待重启”“8502预览”等是当时状态，不代表当前状态。9月15日未发现8501或8502监听服务；下一 session 应重新检查。不要重复开发或修复已经通过验证的内容。

## 1. 接手位置与已完成状态

| 项目 | 当前事实 |
|---|---|
| 本地仓库 | `/Users/gaohongchong/Documents/Codex/LJQCapp` |
| 分支 / 基线 | `codex/v1-2-lj-integration`；当前功能提交 `72cc9d1`，接手时再次核对 |
| Git 交接 | 功能及原交接资料已推送；9月18日新增三阶段任务书及文档更新暂未提交，继续在本目录接手并保留 |
| Python | `/Users/gaohongchong/Documents/Codex/.venvs/ljqcapp/bin/python`，Python 3.13 |
| 本机应用 | `http://127.0.0.1:8501/`；接手时重查8501/8502/8503进程及实际数据库，不依据历史端口用途推断 |
| 当前数据库 | 仓库内 `data/qc_lj_app.db`，已完成生命周期迁移；9月18日只读确认质量目标迁移尚未执行，新代码首次启动自动增量迁移 |
| 已知备份 | `data/backups/before-lot-lifecycle-20260911-142235.db`；实际最新备份在接手时核对 |
| 迁移结果 | 3 次 Z-score 检测、6 条水平结果原值保留；3 条来源上下文回填，实际试剂批号仍未知 |
| 最近测试 | 质量目标阶段18个相关套件通过，末轮质量目标9组及相关回归通过；9月18日仅改文档，检查本地链接及差异，不重跑应用测试 |

已完成：项目文件夹内默认数据库及旧库迁移；模板的仪器/试剂/质控品字典选择；LJ、Z-score、即时法新版配置接入；即时法 3 点 SI、20 点人工转入 LJ；三方法实际试剂批号、换批验证与事件、质控品平行/结束、部分水平新组合、LJ/Z-score 参数版本、逐次快照和报告追溯。

后续已完成术语整理和质量目标首版，范围分别见 [术语与流程实施说明](terminology_workflow_implementation.md)、[质量目标实施说明](quality_targets_implementation.md)。批号边界见 [批号生命周期实现说明](v1_2_lot_lifecycle_implementation.md)。[整体设计](v1_2_lot_lifecycle_design.md) 保留实施前问题和完整目标，不表示每一目标均已交付。

## 2. 新 session 先做什么

1. 按 `AGENTS.md` 顺序阅读 `DEVELOPMENT_HANDOFF.md`、V1.1 数据模型规格、V1.2 LJ 规格、最新实现说明和本任务书，再读 `terminology_and_workflow_alignment.md`。不要只依据旧聊天或旧规格判定功能未做。
2. 检查分支、未提交改动和最近提交，保留现有工作区；不要通过 reset、clean 或重新克隆来“恢复基线”。其他电脑拉取该分支并核对提交；数据库、备份和验收产物需另行迁移，不包含在 Git 中。
3. 检查 8501 是否在运行。已运行则打开现有应用；未运行才用下方命令启动。
4. 阅读迁移审计及最近回归日志。测试用独立临时数据库；不要把测试种子写入当前用户库，也不要清空现有测试记录。
5. 阅读 [质量目标实施说明](quality_targets_implementation.md)，再按文首三阶段安排从 [任务 1](task_01_out_of_control_reports.md) 开始。第 2.1、2.2 及第 3 节是已完成工作的历史验收要求，不从头重做。若用户在新 session 指定不同下一步，以新指令为准。

### 2.1 应用术语和用户界面整理（已完成，历史验收要求）

以术语文档第2节为清单，统一菜单、表单、提示、帮助、图表及报告中对应称呼。先确认字段实际含义，再改名称；不能用文案替换掩盖缺失能力。

- 保留“新建项目”“批次管理”“更换质控品批次”，不恢复“项目模板”“复制上一批次”。
- 质控品的比较称“新旧批号比对”；试剂使用患者样本的比较称“试剂换批比对”。现有同时使用和状态管理不能仅因改名就声称具备完整比对图表、计算及报告。
- “建靶”“靶值版本”对应整理为“均值和标准差建立”“均值和标准差管理”；保留来源、适用时间和历史变更。
- 分开显示批次资料状态、均值和标准差确认状态、质控品使用状态，不能都写“已启用”。区分设定变异系数、实测变异系数和允许不精密度。
- “质量目标”“失控原因分析与处理”“失控报告”“上传附件”使用专业中文；不再给用户显示“结构化闭环”等开发用语。
- 基础统计和计量术语核对适用GB/T、JJF，业务及质量要求按WS/T。已查阅和仍待核对的标准正文范围见术语文档，不能宣称全应用已符合所有标准。

验收：空库和已有批次均可正常操作；旧记录原值及判读不变；三方法及管理页面没有含义冲突的名称。按影响运行已有界面/集成用例并实际浏览器核对。报告实现若有改动，生成并检查PDF；只改文字不应引发数据库字段或历史内容批量重写。

### 2.2 新旧质控品换批全过程核查（已完成本轮核查，历史验收要求）

目标操作：旧批继续日常使用 → 新批同时检测并积累自身数据 → 确定新批适用均值和标准差 → 新批正式使用 → 旧批停止使用。

| 核查项 | 预期与边界 |
|---|---|
| 新批数据衔接 | 正式使用前后的新批属于同一批，已积累数据保留，不重新建立空批；旧批原始结果不并入新批 |
| 同产品换批与更换产品 | 操作上明确区分；同产品换批也需确定适用均值和控制限，不能自动继承旧批均值 |
| 现有两个入口 | 一条能沿用设置建立待确认批次，另一条也能创建或使用合适的新批并记录同时使用；核查如何串联，避免用户重复新建。入口合并尚未完成，不要重写已有底层功能 |
| 已有自动跳转 | “建立新批次草稿”后自动选中新草稿进入核对已实现，保留并验证即可 |
| 标准与现有计算差距 | 逐项核查独立分析批计数、暂定与常用均值/标准差、长期精密度资料利用、稳定性较长/较短质控品的不同方案；先提供代码与标准对照，不把未核查项写成已确认缺陷 |
| 比对图表与报告 | 查实是否有新旧批同时间段的分别统计和图表/报告。状态管理已有，不代表伯乐式比对报告已完整实现 |

发现可复现的数据衔接或流程问题则修复并做针对性验证。不要将20个数据点等同20个独立分析批，也不要在改名时全局更改现有建靶点数和即时法3/20点规则。涉及统计方案的实质变化，先说明适用对象、当前差距及具体改法，再按已确认业务逐项落地。

### 2.3 参考手册位置

- `/Users/gaohongchong/Documents/Codex/LJQCapp/docs/references/manuals/Q-expert软件操作手册.pdf`
- `/Users/gaohongchong/Documents/Codex/LJQCapp/docs/references/manuals/Unity-001 伯乐.pdf`

已按用户要求复制两份完整PDF并核对与OneDrive原文件一致，新session优先使用项目内副本。相关页码、原始来源和迁移方式见[参考资料索引](references/README.md)，官方标准链接已写入术语文档。PDF被`.gitignore`排除，不随公开仓库推送；同机可直接读取，换电脑需另行复制。手册仅供参考，不自动引入其中的网络服务、权限、自动删除/纠正等行为。临时提取文件不能作为跨session的唯一来源。

```bash
cd /Users/gaohongchong/Documents/Codex/LJQCapp
git status --short --branch
git log -1 --oneline
lsof -nP -iTCP:8501 -sTCP:LISTEN
```

仅在应用未运行时启动：

```bash
/Users/gaohongchong/Documents/Codex/.venvs/ljqcapp/bin/python -m streamlit run app.py --server.port 8501 --server.address 127.0.0.1
```

## 3. 9月11日换批核查与开发记录（历史）

本阶段目的是检查现有实现能否顺畅支持用户的实际操作，修复证据明确的问题。以下是待核查项，不是已确认的缺陷列表。

| 优先级 | 核查任务 | 验收结果 |
|---|---|---|
| P0 | 按 LJ、2/3 水平 Z-score、即时法各走一遍试剂换批，再查看原记录、图和导出 | 原项目不重复创建；实际批号保存正确；适用参数和连续规则不因试剂换批被清空 |
| P0 | 同一项目的质控品新旧批平行、正式启用、结束旧批，包括未来生效计划 | 新序列无旧检测结果；状态按生效时间应用；旧批可追溯，结束后写入被阻止 |
| P0 | 复核换批验证的适用范围：不同检测项、后续失败结论、验证日期和效期、单个水平更换 | 不得拿其他项目/批号的验证通过记录启用目标批；检查旧的通过结论能否错误绕过较新的失败结论 |
| P0 | 复核历史补录、参数版本切换、旧表单与导入预览后变更默认批号 | 根据检测时间使用适用批号/版本；陈旧确认被提示；失败事务不留下半条结果 |
| P1 | 走完部分水平换批、未换水平参数预填、全部水平确认的界面链路 | 用户能辨认实际质控批号和参数来源，不把参考值误当作已确认的正式参数 |
| P1 | 当前库历史数据、上游停用和结束批次的查询入口 | 无实际批号的旧数据明确显示未知；原值保留；用户能找到可用的历史查询及报告入口 |
| P1 | 指定高缩放环境与常规显示环境对比 | 重点复测 `2880×1800 @ 200%` 的表单、表格和图例，明确记录是否具备该环境 |

隔离浏览器此前已验证：Z-score 连续保存 3 次后保持合并视图；三条实际试剂批号 R001/R002/R002；检测人输入后按 Enter 保留数值；指定即时法检测系统换批成功。新 session 应聚焦上述未充分走通的边界，避免无目的重跑全部流程。

### 3.1 本次接续结果（2026-09-11）

已复现并修复：

- 质控品正式启用、部分水平换批原先可引用被较新失败结论替代的旧通过记录；现在按生效时间取最新结论，同一时间按记录编号排序，历史生效时间仍可使用当时适用的通过记录。
- 正式启用原先未核对质控品效期和停用状态；现在核对实际批号及水平，效期当日仍有效。
- 混合批号组合原先只检查配置主批号的验证；现在必须逐一关联全部实际批号，事件详情保存完整验证映射，原单批号调用仍兼容。
- 当前可写的新批次原先允许补录开始平行前的检测，或录入未来结束时间之后的检测；现在同时检查当前只读锁和检测时间对应状态，已结束批次不会因补录旧日期而解锁。
- 试剂换批预览原先按最新插入的通过记录展示，可能与历史生效时间不一致；现在先选择启用时间，再展示当时最新验证（包括失败结论）。

验证结果与证据：

| 范围 | 本轮结果 |
|---|---|
| 新增边界测试 | `tests/lot_lifecycle_boundary_smoke_test.py`：9 组通过；最初 5 组在修复前全部失败，复现日志保留 |
| 既有回归 | 批号生命周期 11 项，以及 LJ/Z-score/即时法核心与接入、离群维护、项目配置、配置导入、报告历史、结果迁移；连同新增套件共 12 组 suites 通过 |
| 三方法换试剂 | 自动化验证 LJ、2/3 水平 Z-score 和即时法的项目/序列、参数及逐次批号保留；即时法指定项目切换另完成浏览器提交 |
| 参数与补录 | 自动化验证历史时间选择参数版本、旧导入预览后默认批号变更的拒绝及整批回滚、跨项目/批号验证拒绝、过期验证拒绝 |
| 混合组合界面 | 浏览器完成旧结论拒绝、缺少逐批验证拒绝、原两水平参数预填 100/2 与 200/2、新高水平 300/2 全量确认、两个批号分别关联验证并正式启用 |
| 旧库历史入口 | 在 8501 查看既有批次追溯与导出入口；3 次检测、6 条水平原始记录哈希与迁移审计一致；批号仍未知 |
| 显示环境 | 检查 1440×900 与 2560×1440 有效视口中的换批预览及逐批验证表单；未复现遮挡。未具备指定 2880×1800 @ 200% 硬件/浏览器缩放验收环境，不能用视口测试替代 |

日志、隔离库和浏览器种子脚本位于 `output/lot-boundaries-2026-09-11/`；`regression.json` 记录其中 11 个 suites，既有批号套件单独见 `lot-lifecycle.log`。`user-data-check.json` 保存原始结果核对。旧 PDF 已复制到 `previous-artifacts/` 再运行既有报告验收；本轮未修改报告实现，未重做 PDF 逐页视觉验收。

下一轮重点：补足 LJ 与 2/3 水平 Z-score 从界面换试剂到保存、看图、下载的整条路径；多项目选择性换质控批及未来计划在不同窗口中的实际操作；指定高缩放环境下设置页、记录区和图例专项检查。以上不应写成已确认缺陷。现有自动化和混合组合界面验收无需无目的重跑。

本轮只改服务校验、管理界面及测试，无新增 schema 迁移，无历史数据回写。所有源码改动留在工作区；提交和推送仍按用户后续指令执行。

### 3.2 用户界面整理（2026-09-11，用户新增优先任务）

用户要求已实现：首页说明按当前功能重写；基础资料、批号管理与质控方法入口分开；各页清理开发者说明。

- 首页只保留 LJ、Z-score、即时法三个方法卡片及八项使用指南，覆盖资料准备、人工/厂家参数确认、实际批号、部分水平换批、整次 Z-score 维护、即时法 3/20 点、导入、报告和备份。
- 顶部“资料与批次”包含基础资料和项目/批次管理，点击后自动收起。方法页只选择已有项目与批号。报告历史、设置和帮助保持独立。
- 清理“新版配置、接入、运行时、软停用、压缩展示、同一操作卡”等用户可见实现说明，保留必要业务限制；通过 Streamlit 配置关闭开发工具栏及技能安装提示。
- 历史判读 JSON、靶值版本 JSON、原始事件表改为中文摘要与明细。`ui/traceability.py` 只转换显示，不重算或回写保存内容；展示实际参数版本号，保留数值精度和缺失信息，正式期不将内部建靶保留标记误写为参与建靶。
- 首页移除旧演示刷新入口：原生成器只建旧结构项目，无法出现在当前配置工作台。保留脚本与已有记录；未扩展演示数据生成能力。

验证：8 个 smoke suites 通过（基础资料、项目管理、三方法接入、设置、报告历史及新增用户界面套件）；新增套件核对三方法保存证据、数值精度、未记录标记、原始判读不变及空库全局导航。LJ 集成测试改为验证从顶部进入管理再返回后批号选择仍保留。编译与 `git diff --check` 通过。

浏览器已检查首页、三方法选择页、LJ 当前批次、基础资料、项目管理、批号/靶值/历史入口及设置；最终版另核对菜单自动收起和中文判读表。检查 1440×900 与 2560×1440 有效视口；未提供真实 2880×1800 @ 200% 环境，仍保留原专项验收项。未修改 PDF 生成逻辑，报告只调整页面展示文案。

日志、隔离数据库及启动入口：`output/ui-cleanup-2026-09-11/`。**8502 为新版隔离预览，使用测试库；8501 仍是用户原库服务，已确认模块仍缓存旧界面。**自动审批拒绝终止 8501 进程，理由是可能丢失未保存表单；重启待用户明确批准，不得绕过。无需数据库迁移。当前改动继续留在工作区，未提交/推送。

### 3.3 项目与批次术语统一（2026-09-11，用户截图反馈）

用户明确：管理页第一、第二页签分别为“新建项目”和“批次管理”；原“项目模板”是业务项目，不能继续作为模板呈现。

- 全应用业务文案统一为项目、项目名称、项目备注、选择/启用/停用/恢复项目；项目内部条目称“检验项目”。批号配置统一显示为批次，补齐批次名称、新建批次、复制上一批次、方法页的“项目与批次”等名称。
- 同步首页、基础资料提示、三方法选择页、换批提示、服务校验错误、README 和 XLSX 导出元信息。项目导出的“模板信息”表改为“项目信息”，批次导出的“批号信息”表改为“批次信息”；导入数据表“项目配置”不变，旧/新描述表名称都能导入。
- CSV/XLSX 的“导入模板”仍保留其原意。数据库表名、Python 接口与控件 key 保持兼容，不重命名用户已有项目、不回写历史变更说明，没有 schema 迁移。
- 7 个相关 suites 通过：项目管理、项目导入导出、LJ/Z-score/即时法接入、用户界面、生命周期边界。复用了实际项目创建和返回后批次保持用例；导入导出补充新旧描述表兼容验证。
- 浏览器确认新建项目、批次管理及导入导出页面，包括展开表单的项目名称/备注、批次名称和导航菜单。日志位于 `output/project-terminology-2026-09-11/`。新版 8502 独立测试预览已重新加载；8501 未重启，仍按第 3.2 节等待用户批准。未提交或推送。

用户后续界面精简：按截图移除项目管理页整块“推荐顺序”提示及“可选资料”数量统计。编译、差异检查和实际浏览器确认通过；8502 已加载，8501 状态不变。

### 3.4 CV 建立与后续显示对齐（2026-09-11，用户新增任务）

按用户要求查阅 Westgard 与 NIST 后，补齐项目添加、批次创建时的 CV 要求上限和依据；草稿可修订、复制继承、已启用批次冻结。均值和 SD 自动计算靶值 CV%，与实测建靶/实时 CV 分开。批次管理和参数确认默认展示当前生效版本；LJ 不再使用最后一条旧版本记录覆盖当前参数显示。Z-score 正式参数增加比较，参数历史及批次 XLSX 增加 CV 列，三方法和月报统一计算，非正均值不再输出负 CV 达标提示。

新增 CV 流程套件及 13 个相关既有 suites 通过，覆盖 UI 建立→批次→工作台、版本修订后显示一致和旧判读不变。修正既有测试中的过期中文断言，冷启动字体缓存超时通过合理超时和复用缓存解决。PDF 的 10 页回归输出已渲染核对。详细范围、依据和日志见 [CV 建立与显示口径](cv_setup_alignment.md)。无 schema 迁移或用户库修改。8502 已改用本轮独立测试库，8501 不变，仍待用户批准重启。

### 3.5 旧批次设置复用流程（2026-09-11，用户截图反馈）

用户表示“复制上一批次”用途难以理解。入口改为“更换质控品批次”，明确用于换用新质控品批号时省去重填设置；先选择旧批次，再选择新质控品批号，显示检验项目、质控方法、水平数、CV 上限及靶值处理方式，按钮为“建立新批次草稿”。去除整块蓝色长说明，改为用途及记录保留的简短说明。

新批号选择排除旧批号及该项目已建立普通批次的批号；无可用批号时提供基础资料入口。服务层拒绝普通复制到同一质控批号；部分水平更换的组合复制仍按原规则允许使用未换的主批号。创建成功后自动切换到“批次管理”，选中刚建立的草稿并提示核对。未扩展正式换批/验证功能，不自动启用新批次。

新增 `tests/copy_batch_workflow_smoke_test.py` 验证不同批号限制、真实界面创建及自动跳转、草稿与参考值待确认、旧记录不变；项目管理、CV、生命周期边界及用户界面 4 个既有 suites 通过。全页面回归同时修复水平组合选项格式函数晚绑定导致的回放取错水平问题。日志及隔离预览位于 `output/copy-workflow-2026-09-11/`，8502 使用本轮测试副本；8501 原用户服务状态不变。无 schema 或历史数据修改，未提交/推送。

浏览器已完成从旧 LJ 批次选择到新质控批号 `QA-COPY-NEW` 的实际草稿创建，确认旧批号被排除、CV 5% 及重新建靶预览正确，提交后自动选中新草稿并停留在核对页面。1280×720 有效视口表单、预览及按钮无重叠；无可选批号入口由 AppTest 覆盖。未改 PDF 逻辑，未重复报告测试。

## 4. 用户选定的后续功能与范围

### 4.1 任务 1：失控原因分析与报告

按 [任务 1 完整规格](task_01_out_of_control_reports.md) 实现。先做 LJ / Z-score 正式期事件关联、分析措施、复测和人工效果确认，再打通附件、备份恢复、独立 PDF 及历史。先完成这一条完整操作路径，再推进总览与多项目录入。

### 4.2 任务 2：今日质控总览与多项目操作

按 [任务 2 完整规格](task_02_daily_workbench.md) 实现。复用任务 1 的未处理查询，先做总览与直达，再做多项目录入和设置衔接。必须实际验证 Enter/Tab/粘贴、草稿保留、完整 run 保存、失败重试和视图状态。

### 4.3 任务 3：换批比对与批量月报

按 [任务 3 完整规格](task_03_comparisons_and_monthly_reports.md) 的 3A、3B、3C 顺序交付。质控品新旧批号分别统计、同患者样本试剂配对比较、批量月报使用各自明确的口径，不能套同一个通用通过阈值。

### 4.4 原有已选功能的保留范围

下表保留9月15日确认的业务边界。其中质量目标首版已完成，附件并入任务 1，失控报告对应任务 1，试剂换批对应任务 3。历史批号更正仍是已选后续需求，但不为凑齐功能而插入三阶段的关键路径。独立分析批、暂定/常用参数、长期/加权 CV 等已有差距另列后续统计方案，不能因完成体验改进就标为已实现。

| 功能 | 具体交付与验收边界 |
|---|---|
| 历史检测记录批号更正 | 从某次检测进入更正，保存原批号、新批号、原因、更正人及时间；更改当前默认批号不能回写旧结果。质控品批号更正若影响参数和判读，须明确核查和更正处理，不能静默改写原判读 |
| 上传附件 | 放在换批验证或失控报告中，保存已有表格、扫描件等；支持查看/下载并纳入备份恢复。上传不代表计算或验证通过；文件格式、大小及保存方式属于待实现细节，无需单独业务入口 |
| 试剂换批比对 | 录入同一患者样本用两批试剂得到的成对结果，计算差异并输出报告；按明确的适用标准或实验室方案判断。与两批不同质控材料的新旧批号比对分开，不扩展为通用性能验证平台，不预设万能合格阈值 |
| 质量目标 | 按适用WS/T预置/导入，优先核对WS/T 403—2024与406—2024；支持检验项目匹配、单位、浓度区间、允许不精密度、允许偏倚、允许总误差、标准版本和实施日期。已内置WS/T 886项目名称代码不代表目标库已做；标准缺项不能臆造，TEa不能直接当CV上限，旧批次保留原采用要求 |
| 失控原因分析与报告 | 从原失控记录填写原因分析、纠正措施、复测结果、处理效果和必要的患者结果影响评估，保存/查询/导出失控报告；保留原失控记录，不以“处理完成”覆盖原判断。填写确认人不等同引入权限与电子签名系统 |

本轮未纳入：即时法月报、LIS、权限与电子签名、安装包发布，以及手册里的其他功能。数据库迁移、原始结果变更、报告生成和附件备份均按实际影响单独验证，不能把需求清单直接写成完成清单。

## 5. 代码定位

| 范围 | 主要文件 |
|---|---|
| 迁移和逐次数据写入 | `migrations/v1_2_lot_lifecycle.py`、`scripts/migrate_lot_lifecycle.py`、`database.py` |
| 批号、验证、状态、参数版本和追溯 | `services/lot_lifecycle_service.py` |
| 管理与录入界面 | `pages/lot_lifecycle_section.py`、`pages/project_management_page.py`、三方法页面及 sections |
| 配置、水平组合、工作台绑定 | `services/project_config_service.py`、`services/workbench_config_service.py`、`services/zscore_workbench_service.py`、`services/instant_workbench_service.py` |
| 按版本计算与转换 | `qc_logic.py`、`zscore_logic.py`、`services/instant_service.py` |
| 图表与报告 | `plotting.py`、`zscore_plotting.py`、`services/report_service.py`、`services/report_pdf_layout.py` |
| 批号验收 | `tests/lot_lifecycle_smoke_test.py` |

## 6. 验证及交付要求

按改动范围运行相关 smoke tests。当前虚拟环境未安装 pytest，仓库测试可直接运行；示例：

```bash
MPLCONFIGDIR=output/zscore-v12-2026-09-11/mpl-cache /Users/gaohongchong/Documents/Codex/.venvs/ljqcapp/bin/python tests/lot_lifecycle_smoke_test.py
```

三方法规则和维护改动需追加对应核心及接入测试；报告改动需生成、渲染并检查 PDF；界面改动需实际浏览器验证。现有批号测试会在 `output/lot-lifecycle-2026-09-11/` 更新验收 PDF，重要旧验收产物需保留时先复制到新的验收目录。

完成时记录：复现场景、修复行为、原始数据是否受影响、执行的验证及未验证环境。更新本任务书、README 和开发交接说明。数据库及备份、运行日志不进入 Git；仅本地修改不等于已同步到 GitHub，提交/推送按用户后续指令执行。
