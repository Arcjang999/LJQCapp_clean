# R0-01｜接续前的既有业务链核验

- 原任务：任务 0 后的既有流程验收；来源：[下一阶段清单 §3](../../NEXT_SESSION_TASKS.md#3-整体验收清单)。
- 状态：未开始（已有单项证据可以复用，不能重复声称新运行）。
- 类型：验收；前置：读任务 0 [本轮复核记录](../../task_00_reaudit_2026_09_22.md)。
- 本卡交付：核对当前版本并完成本机可执行的连贯业务链，给 T1-01 一个准确基线。
- 公约：[逐卡执行规则](../EXECUTION_RULES.md)。

## 1. 目标与用户可见结果

接着已经实现的任务 0 开发，不重新做目录、搜索和旧管理弹窗。确认后续失控处理可以建立在正确的现有记录和判读上。

## 2. 必读文件与入口

- `docs/task_00_reaudit_2026_09_22.md`、`docs/DEVELOPMENT_HANDOFF.md` 当前状态。
- `app.py`、`pages/main_page.py`、三方法工作台、现有生命周期和报告/存储服务，仅沿验收路径定位。
- 当前工作区与真实预览进程；不能假设文档中的 8505 仍运行或仍指向相同数据库。

## 3. 逐步执行

1. 记录分支、当前提交与未提交修改，保留用户工作。既有任务 0 改动不因本卡还原、清理或重新导入旧目录。
2. 选独立库与明确的预览入口，确认没有绑定真实用户数据库。任务 0 的当前证据适用且代码未变的单项直接引用；标明引用而非本次运行。
3. 走同一业务数据链：项目→混合检测项→具体批次→水平/实际材料→质量要求核对→参数/使用状态→录入→图→导出→报告历史→备份恢复。
4. 覆盖 LJ、2/3 水平、即时法3/20点既有路径，验证实际批号与参数来源、多水平完整run、返回/筛选及草稿。标准适用与原算法结果不因导航变化而改变。
5. 覆盖试剂未来使用安排/取消/实际使用事件更正，质控品完整与部分水平换批；确认原结果不被复制或回写。
6. 检查常规浏览器布局、中文业务用语和模糊搜索。全系统200%及Windows打包的未验收部分单列到 R0-02，不用小视口冒充。
7. 发现直接阻挡上述业务链的问题，在本卡内修最小回归并补对应验证；不以验收为由重构系统或扩大标准覆盖。需新的业务规则时只记录差异并按决策流程处理。

## 4. 不做

- 不重开任务0研究，不凑项目数，不增加未来/废止来源。
- 不开始任务1实现，不要求任务2B/4/5已完成才能验收当前软件。
- 不把缺少Windows或真实缩放设备作为本机功能开发的停工条件；仍不得宣布发布验收完成。

## 5. 验收案例

R0-01-A1：给定同一项目内LJ、2水平、3水平和即时法检测项，从各自批次连续录入并返回，入口、完整run、图和报告引用正确。

R0-01-A2：给定未完成适用质量要求核对的新批，直接录入或导入被统一服务拒绝；补齐后可沿原路径继续，不能另开宽松入口。

R0-01-A3：给定材料或试剂未来切换，取消不会写入；已保存的原检测仍显示实际历史来源。

R0-01-A4：给定当前机器没有指定Windows/缩放环境，本机证据可以通过并继续 T1-01，R0-02 状态仍明确为待环境。

## 6. 证据与完成条件

在 `docs/execution/deliveries/R0-01.md` 保存环境、复用证据、本次操作结果和已修缺陷。只有本机可执行链路完成且无阻断缺陷才能通过；未获得的发布环境证据另列。

## 7. 停止位置

本卡完成后停止；下一张 T1-01。用户后续明确启动该卡时再实现失控处理。
