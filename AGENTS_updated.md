# AGENTS.md

## 1. 项目定位

这是一个**本地单机版实验室室内质控软件**，当前技术栈为：

- Python
- Streamlit
- SQLite
- pandas
- matplotlib

当前版本已经不是“仅有单页原型”的状态，而是一个**可打包、可本地运行、可实际试用的 V1 基线**。现有正式入口包括：

- 主页 / 说明页
- 单水平（LJ法）
- 多水平（Z-score法）
- 即时法
- 设置页
- 报告历史页

当前开发原则是：**在现有可运行基线上持续增量修正和完善，不做与当前里程碑无关的大重构。**

---

## 2. 当前仓库真实结构

### 2.1 顶层入口

- `app.py`：总路由入口；负责主页、LJ、Z-score、即时法四个主入口切换，并通过全局状态打开设置页和报告历史页。
- `run_app.py`：本地启动 / 打包后启动入口。

### 2.2 页面层

- `pages/main_page.py`：主页 / 产品说明 / 入口说明
- `pages/lj_page.py`：LJ 工作台页
- `pages/zscore_page.py`：Z-score 工作台页
- `pages/instant_page.py`：即时法工作台页
- `pages/settings_page.py`：设置页
- `pages/report_history_page.py`：报告历史页
- `pages/management.py`：项目/批次管理通用流程
- `pages/lj_sections.py`：LJ 业务区块
- `pages/zscore_sections.py`：Z-score 业务区块
- `pages/lj_report_section.py`：LJ 报告相关展示/导出区块
- `pages/zscore_report_section.py`：Z-score 报告相关展示/导出区块

### 2.3 逻辑 / 服务层

- `database.py`：数据库初始化、迁移、数据访问函数
- `qc_logic.py`：LJ 规则、统计与维护逻辑
- `zscore_logic.py`：Z-score 规则、统计与维护逻辑
- `plotting.py`：LJ / 通用绘图函数
- `zscore_plotting.py`：Z-score 绘图函数

已存在并应优先复用的服务模块：

- `services/instant_service.py`
- `services/outlier_service.py`
- `services/report_service.py`
- `services/report_pdf_layout.py`
- `services/settings_service.py`
- `services/storage_service.py`
- `services/value_type_service.py`

### 2.4 UI 公共层

- `ui/common.py`
- `ui/dialogs.py`

### 2.5 测试

当前仓库已存在 smoke tests，不要忽略它们：

- `tests/instant_smoke_test.py`
- `tests/building_outlier_smoke_test.py`
- `tests/lj_monthly_report_smoke_test.py`
- `tests/zscore_monthly_report_smoke_test.py`
- `tests/report_history_smoke_test.py`
- `tests/settings_smoke_test.py`
- `tests/storage_smoke_test.py`
- `tests/results_migration_smoke_test.py`
- `tests/zscore_smoke_test.py`

> 注意：仓库中的部分旧文档可能落后于代码现状。若文档与实际代码冲突，**优先以当前可运行代码、现有 tests、用户最新明确口径为准**。

---

## 3. 当前产品目标（V1）

当前 V1 的优先目标不是管理平台，也不是多用户系统，而是先完成一个：

- 可打包
- 可本地运行
- 可实际使用
- 可导出标准报告
- 可完成核心室内质控流程

V1 目标包括：

- 支持项目 / 批次管理
- 支持 LJ 方法
- 支持 Z-score 方法
- 支持 即时法（单水平，格拉布斯法）
- 支持建靶期离群值处理
- 支持标准月度质控报告 PDF
- 支持系统设置页（实验室信息、报告声明、数据库位置、备份恢复）
- 支持报告历史查看与下载

---

## 4. V1 明确范围

### 4.1 V1 必做 / 现有基线内持续完善

- 项目级输入值类型
- 即时法模块
- LJ 建靶期离群值处理
- Z-score 建靶期离群值处理
- 月度质控报告 PDF
- 报告历史页
- 设置页
- 数据库位置迁移
- 备份恢复

### 4.2 V1 不做

- 性能验证
- 管理框架 / 多用户 / 权限
- 云端部署
- peer-group
- COA 自动解析
- 自定义 Word 模板编辑器
- 与当前里程碑无关的大规模架构重写

### 4.3 暂不纳入 V1 的词汇/模块

- 不再使用“QCI”作为当前方法名
- 当前方法扩展统一叫“即时法”
- 若需扩展复合质控，放到后续版本

---

## 5. 已锁定的产品规则

### 5.1 项目级输入值类型

项目创建时必须单选以下其中一种：

- 真实检测值
- Ct值
- log值

规则：

- 一个项目只能有一种输入值类型
- 批次继承项目的输入值类型
- 批次层不允许改单值类型
- 录入页字段名、CSV 模板表头、图表纵轴、报告字段名都必须跟项目值类型一致
- 不要再把“录入页实时 log10 换算”作为主要交互模式
- 旧数据兼容可以保留底层字段，但新 UI 不应继续强调“边输入边换算”

### 5.2 即时法

即时法规则已锁定：

- 单水平
- 使用格拉布斯法
- 有 3 个**有效点**后即可开始检验
- 累计到 20 个**有效建靶点**后，提示用户可转入 LJ
- 转入 LJ 必须是“提示 + 人工确认”，不是自动转入
- 即时法已经是正式工作页，不再视为占位入口

### 5.3 建靶期离群值处理（通用）

离群值处理适用于：

- LJ 建靶期
- Z-score 建靶期

统一规则：

- 默认方法先按格拉布斯法实现
- 代码层可以预留其他方法扩展接口，但 V1 UI 不开放方法切换
- 系统发现疑似离群值后，只做提醒，不自动剔除
- 用户可选择：保留 / 禁用 / 恢复
- 禁用不删除原始记录
- 禁用后不参与建靶统计
- 禁用后默认不显示在主图上
- 禁用点必须在数据表、维护区、报告附表中可见
- 页面摘要中要明确显示：
  - 总记录数
  - 有效建靶点数
  - 已禁用点数

### 5.4 Z-score 的最终判定与维护粒度

这一条已经按最新产品口径锁定，**不要继续按旧 level 粒度理解**。

#### 最终判定粒度

- 多水平 Z-score 的**最终判定单位是 run**，不是各 level 各自独立给最终结论
- 任一 level 触发 **reject 类规则** 时，整次 run 视为触发 / 失控
- 其他 level 的状态仍要展示，但属于“明细证据”，不改变整次 run 的最终结论

#### 建靶维护粒度

- Z-score 建靶期的保留 / 禁用 / 恢复，目标规格应按**整个 run**处理
- 不再按单个 level 分别维护
- 也就是说，同一条 run 下的多个 level 应联动保留、联动禁用、联动恢复

#### 当前代码现状与改造原则

- 当前代码中部分实现仍按 level 粒度维护
- 涉及位置主要在：
  - `pages/zscore_sections.py`
  - `zscore_logic.py`
- 若任务要求改为 run 粒度，应在现有逻辑上做**渐进式迁移**，不要重写整个 Z-score 模块

### 5.5 正式期后的建靶维护锁定

一旦 Z-score 批次进入正式期：

- 建靶期的“保留 / 禁用 / 恢复”维护区域应**隐藏**，而不是继续显示可操作按钮
- “打开维护记录”后，仍可查看建靶期数据
- 但这些建靶期记录应为**只读**：
  - 不能维护
  - 不能删除
  - 不能继续改保留/禁用状态

对 LJ / Z-score 正式期锁定类需求，也应尽量复用现有锁定判断，不要再造第二套口径。

### 5.6 图表与视图行为

#### 多水平视图模式保持

- 在多水平 Z-score 页，如果用户当前选择的是“合并视图”，录入并保存新检测值后，右侧图表应继续保持“合并视图”
- 不应在每次保存后回到默认的“单水平视图”
- 视图状态应优先沿用现有 `session_state` 机制做修复，不要推翻重做整个状态系统

#### 合并视图图例遮挡

- 多水平 Z-score 合并视图中，图例不应遮挡高值 level 曲线或关键标注
- 优先考虑把图例移到图外或改为更稳妥的位置，而不是只临时挪一点点坐标

#### 月图图例必须和实际绘图阶段一致

- 如果月度图只画正式期数据，就不要显示建靶期图例
- 如果导出的建靶期图只画建靶数据，就不要显示正式期图例
- 图里没画什么，就不要在图例里展示什么

#### 手动备注显示方式

- 图上继续使用“描边点 = 含手动备注”的提示方式
- 当前版本**不要求**把备注文字直接铺在图上

### 5.7 高缩放 / 高分辨率适配

已知复现环境：

- 分辨率：`2880 × 1800`
- 浏览器缩放：`200%`

在该环境下，存在需要优化的页面：

- 设置页局部文字超出格子
- 当前批次检测记录区域可读性差、布局拥挤

而以下环境目前用户侧未复现：

- `2560 × 1440 @ 100%`
- `2560 × 1600 @ 150%`

因此，这类问题应理解为：

- 不是全局必现逻辑 bug
- 是**特定高缩放场景下的布局适配问题**
- 修复时必须保留常规缩放场景的稳定性，不要为了修 200% 缩放而破坏普通布局

### 5.8 报告

V1 报告先做成**固定模板的月度质控报告 PDF**。

报告至少包含：

- 实验室信息
- 项目 / 批次 / 方法
- 输入值类型
- 当前靶值来源
- 月度质控图
- 月度在控 / 失控汇总
- 原始统计 / 有效统计
- 异常 / 失控事件表
- 原因与纠正措施
- 月度结论
- 禁用/剔除记录附表（如适用）
- 报告声明
- 生成时间 / 软件版本

其他规则：

- V1 不需要多套复杂模板
- V1 不需要 Word 模板编辑器
- 异常/失控表的样式与字段，可参考当前 LJ 与 Z-score 已有月报输出风格
- 报告历史页已经是正式功能，生成新报告时应尽量保持历史可追溯
- 若涉及中文字体缺失问题，优先区分：
  - 页面显示问题
  - 本地导出问题
  - 服务器 / 打包环境字体缺失问题

### 5.9 设置页与存储

数据库位置迁移必须放在“设置页”中完成，不允许再做成：

- 卡住主要业务页面的流程
- 要求用户手工输入绝对路径的流程

设置页中的数据库位置迁移应遵循：

- 显示当前数据库位置
- 允许用户通过文件夹选择器选择目录
- 迁移前校验目标目录可写
- 自动复制当前数据库
- 校验复制成功
- 保存设置
- 提示重启应用后生效

设置页还要支持：

- 实验室信息
- 报告声明
- 数据备份
- 数据恢复

---

## 6. 当前代码理解（供 Codex 避免误判）

### 6.1 现有代码不是“仅 app.py 单页”

当前仓库已经是多页面 + service/logic 分层结构：

- 不要假设所有逻辑都还在 `app.py`
- 不要把已经拆分出去的逻辑重新塞回主入口

### 6.2 报告历史页已经是正式模块

- `pages/report_history_page.py` 是现有正式页面
- `services/report_service.py` 中已有相关报告记录能力
- 如果任务涉及报告重生、历史下载、报告追溯，应优先沿现有结构扩展

### 6.3 即时法已经接入正式业务流

- 不要再把即时法当作仅展示文案的占位页
- 若有需求涉及即时法，应优先检查：
  - `pages/instant_page.py`
  - `services/instant_service.py`
  - 对应数据库表与转入 LJ 逻辑

### 6.4 多水平视图状态问题应在现有状态机制上修

若任务涉及“合并视图保存后回跳单水平视图”等问题，优先检查：

- `pages/zscore_sections.py`
- 与 `zscore_view_mode`、`zscore_view_mode_widget` 相关的 `session_state` 逻辑

目标是修复当前状态恢复链路，而不是重写整页视图管理方式。

### 6.5 图例 / 图表遮挡问题主要在绘图层修

- Z-score 图例遮挡、阶段图例不收缩等问题，优先检查 `zscore_plotting.py`
- LJ 图 / 报告图相关问题，优先检查 `plotting.py` 及对应报告 section

### 6.6 正式期维护锁定已有基础实现

- 若要做“正式期后隐藏建靶维护区、只保留只读查看”，优先复用现有锁定判断与维护对话框能力
- 不要另外发明一套独立的“只读规则”绕开现有代码
- 重点位置通常包括：
  - `zscore_logic.py`
  - `ui/dialogs.py`
  - `pages/zscore_sections.py`

---

## 7. 实现原则

### 7.1 先做增量改造，不做大重构

除非任务明确要求，否则不要进行与当前里程碑无关的大规模重构。

### 7.2 页面层不要继续变重

当前项目中已有导出逻辑、管理逻辑、维护逻辑分散在多个页面模块中。新增复杂逻辑时：

- 优先复用现有 service / logic 层
- 尽量不要把大量业务判断继续堆进 `pages/*.py`

### 7.3 追溯优先

任何“禁用”“剔除”“转入 LJ”“生成报告”的动作，都必须尽量保留可追溯信息。

- 不要通过直接删除原始数据来实现产品功能
- 不要为了“看起来干净”而丢失历史记录

### 7.4 图面要干净

- 默认主图只显示当前生效的建靶点和正式质控点
- 禁用点不默认显示在主图里
- 图例不要遮挡关键数据
- 不要为了展示更多信息把图堆得过满

### 7.5 优先修现有行为，不优先推翻现有交互

像以下问题，优先做“修复现有实现”：

- 视图模式保持
- 维护锁定
- 图例位置
- 月图图例动态收缩
- 高缩放布局适配

不要一上来把整个页面换成新框架或彻底重排全部交互。

### 7.6 涉及浏览器行为的问题，必须做手工回归

以下类型的问题，**不能只靠 py_compile 或 smoke test 就宣称完成**：

- 输入框按 Enter 是否清空未保存内容
- 页面缩放 / 高分辨率下的溢出与拥挤
- 视图模式是否在保存后保持
- 图例是否遮挡曲线

这类问题必须补充手工回归结论。

---

## 8. 当前开发优先顺序

如无用户临时插单，优先按以下顺序理解当前工作：

### P0：先定规格 / 先保证口径一致

1. Z-score 多水平最终判定按 **run**
2. Z-score 建靶维护按 **run**
3. 进入正式期后隐藏建靶维护，维护记录只读
4. 月图图例与实际绘图阶段一致
5. 异常 / 失控汇总表口径统一

### P1：高影响 bug / 高频使用问题

1. 多水平 Z-score 保存后保持当前视图模式
2. 多水平合并视图图例不要遮挡高水平曲线
3. 高缩放环境下设置页 / 检测记录区布局适配
4. 输入框按 Enter 清空未保存内容的复测与修复（若复现）

### P2：后续增强 / 后置问题

1. 中文字体缺失的导出环境修复
2. 更细化的报告样式增强
3. 与当前版本边界无关的高级流程

如用户任务与此顺序冲突，以用户明确任务为准。

---

## 9. 测试与验证

### 9.1 代码检查

完成修改后，优先运行当前仓库已有检查方式。至少包括：

```powershell
python -m py_compile app.py database.py qc_logic.py plotting.py zscore_logic.py zscore_plotting.py run_app.py
```

如改动涉及具体页面或服务，建议补充对应模块到 `py_compile` 列表。

### 9.2 Smoke tests

按任务范围尽量运行相关 smoke tests，例如：

```powershell
python tests/zscore_smoke_test.py
python tests/building_outlier_smoke_test.py
python tests/instant_smoke_test.py
python tests/lj_monthly_report_smoke_test.py
python tests/zscore_monthly_report_smoke_test.py
python tests/report_history_smoke_test.py
python tests/settings_smoke_test.py
python tests/storage_smoke_test.py
python tests/results_migration_smoke_test.py
```

不是每次都必须全跑，但改到哪块就要优先回归哪块。

### 9.3 必做手工回归（按任务适用）

如果任务涉及页面流程或浏览器交互，至少补充以下路径中的相关项：

#### Z-score

- 在“合并视图”下连续保存 2–3 条新 run，确认不会跳回单水平视图
- 检查图例是否遮挡高值水平曲线
- 检查正式期后建靶维护区是否隐藏
- 打开维护记录，确认建靶期数据可看但不可维护、不可删除

#### LJ / Z-score 建靶维护

- 禁用后不删除原始记录
- 禁用后不参与建靶统计
- 恢复后统计重新计算正确

#### 输入框 / 浏览器行为

- 在录入框中按 Enter，确认是否存在未保存内容被清空的问题
- 若问题只在特定环境出现，应写清复现环境

#### 高缩放场景

重点检查：

- `2880 × 1800 @ 200%`

并与以下未复现环境对比：

- `2560 × 1440 @ 100%`
- `2560 × 1600 @ 150%`

### 9.4 报告相关验证

若任务涉及报告：

- 检查图例是否与图中实际阶段一致
- 检查异常 / 失控表字段是否正确
- 检查报告历史记录是否保留
- 检查中文字体是否正常（若本轮涉及字体问题）

---

## 10. 提交要求

提交结果时请尽量说明：

- 改了哪些文件
- 为什么这样改
- 是否有数据库迁移
- 是否影响旧数据兼容
- 跑了哪些检查 / smoke tests
- 做了哪些手工回归
- 当前还留了什么后续事项

若只做了部分工作，必须明确：

- 哪些已完成
- 哪些未完成
- 哪些是规格待确认，而不是代码未写

---

## 11. 禁止事项

- 不要重新引入“录入时实时切换 / 计算值类型”的主交互
- 不要把数据库位置迁移做成要求用户手输路径的流程
- 不要为了实现离群值处理而删除原始记录
- 不要把禁用点默认继续画在主图里
- 不要把“性能验证”带回 V1 范围
- 不要因为修一个显示问题就推翻整个页面结构
- 不要把已存在的 service / logic / dialog 能力绕开后再重复造一套
- 不要把文档里的旧口径（如 Z-score 按 level 维护、即时法是占位页）继续当成当前真规格
