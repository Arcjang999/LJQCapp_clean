# AGENTS.md

## 1. 项目定位

这是一个**本地单机版实验室室内质控软件**，当前技术栈为：

- Python
- Streamlit
- SQLite
- pandas
- matplotlib

当前仓库已经有以下主入口和模块：

- `app.py`：顶层入口
- `pages/lj_page.py`：LJ 工作台
- `pages/zscore_page.py` / `pages/zscore_sections.py`：Z-score 工作台
- `pages/main_page.py`：主页/说明页
- `Instant` 当前仍是预留入口，后续将改为“即时法”

## 2. 当前产品目标（V1）

本仓库当前优先目标不是管理平台，而是先完成一个**可打包、可本地运行、可实际使用的单机版 V1**。

V1 的目标是：

- 支持项目 / 批次管理
- 支持 LJ 方法
- 支持 Z-score 方法
- 支持 即时法（单水平，格拉布斯法）
- 支持建靶期离群值处理
- 支持标准月度质控报告
- 支持系统设置页（实验室信息、报告声明、数据库位置、备份恢复）

## 3. V1 明确范围

### 3.1 V1 必做
- 项目级输入值类型
- 即时法模块
- LJ 建靶期离群值处理
- Z-score 建靶期离群值处理
- 月度质控报告 PDF
- 设置页
- 数据库位置迁移
- 备份恢复

### 3.2 V1 不做
- 性能验证
- 管理框架 / 多用户 / 权限
- 云端部署
- peer-group
- COA 解析

### 3.3 暂不纳入 V1 的词汇/模块
- 不再使用“QCI”作为当前方法名
- 当前方法扩展统一叫“即时法”
- 如需扩展复合质控，放到后续版本

## 4. 已锁定的产品规则

### 4.1 项目级输入值类型
项目创建时必须单选以下其中一种：

- 真实检测值
- Ct值
- log值

规则：

- 一个项目只能有一种输入值类型
- 批次继承项目的输入值类型
- 批次层不允许改单值类型
- 录入页字段名、CSV 模板表头、图表纵轴、报告字段名都必须跟项目值类型一致
- 不要再在录入页保留“实时 log10 计算”作为主要交互模式
- 旧数据兼容可以保留底层字段，但新 UI 不应继续强调“边输入边换算”

### 4.2 即时法
即时法规则已锁定：

- 单水平
- 使用格拉布斯法
- 有 3 个有效点后即可开始检验
- 累计到 20 个**有效点**后，提示用户可转入 LJ
- 转入 LJ 必须是“提示 + 人工确认”，不是自动转入

### 4.3 离群值处理
离群值处理适用于：

- LJ 建靶期
- Z-score 建靶期

规则：

- 默认方法先按格拉布斯法实现
- 代码层可以预留狄克逊法扩展接口，但 V1 UI 不开放方法切换
- 系统发现疑似离群值后，只做提醒，不自动剔除
- 用户可选择：保留 / 禁用 / 恢复
- 禁用不删除原始记录
- 禁用后不参与建靶统计
- 禁用后默认不显示在图上
- 禁用点必须在数据表、维护区、报告附表中可见
- 页面摘要中要明确显示：
  - 总记录数
  - 有效建靶点数
  - 已禁用点数

### 4.4 Z-score 离群值粒度
Z-score 的离群值处理默认按 **单 level 点** 处理，而不是整条 run 一起禁用。

原因：

- 当前 Z-score 的建靶、target profile、ready 判断都是按 level 粒度累计
- 不要因为一个 level 点异常而浪费整条 run 的其他 level 数据

### 4.5 报告
V1 报告先做成**固定模板的月度质控报告 PDF**。

报告至少包含：

- 实验室信息
- 项目 / 批次 / 方法
- 输入值类型
- 当前靶值来源
- 月度质控图
- 月度在控 / 失控汇总
- 原始统计
- 有效统计
- 失控事件
- 原因与纠正措施
- 月度结论
- 禁用/剔除记录附表
- 报告声明
- 生成时间 / 软件版本

V1 不需要先做 Word 模板编辑器，也不需要多套复杂模板。

### 4.6 设置页
数据库位置迁移必须放在“设置页”中完成，不允许再做成：

- 卡住主要业务页面的流程
- 让用户手工输入绝对路径的流程

设置页的数据库位置迁移应遵循：

- 显示当前数据库位置
- 允许用户通过文件夹选择器选择目录
- 迁移前校验目标目录可写
- 自动复制当前数据库
- 校验复制成功
- 保存设置
- 提示重启应用后生效

同时设置页还要支持：

- 实验室信息
- 报告声明
- 数据备份
- 数据恢复

## 5. 当前代码理解（供 Codex 避免误判）

当前仓库中：

- `app.py` 仍是顶层单页面入口
- LJ 与 Z-score 已经有较完整的工作台结构
- 目前 LJ / Z-score 页面仍存在“输入值 + 实时 log10 提示”的旧交互
- `Instant` 当前仍是占位，不是正式业务页面
- 导入导出能力已经存在，但“标准报告中心”还未独立成模块

开发时请以“在现有基线上渐进演进”为原则，不要推倒重做整个应用框架。

## 6. 实现原则

### 6.1 先做增量改造，不做大重构
除非任务明确要求，否则不要进行与当前里程碑无关的大规模重构。

### 6.2 页面层不要继续变重
当前项目中已有部分导出逻辑直接写在页面模块里。新功能应优先抽到 service / logic 层，避免继续把复杂逻辑堆到 `pages/*.py`。

建议优先新增或复用的模块方向：

- `services/value_type_service.py`
- `services/instant_service.py`
- `services/outlier_service.py`
- `services/report_service.py`
- `services/settings_service.py`

### 6.3 追溯优先
任何“禁用”“剔除”“转入 LJ”“生成报告”的动作，都必须尽量保留可追溯信息。不要通过直接删除原始数据来实现产品功能。

### 6.4 图面要干净
默认图表只显示当前生效的建靶点和正式质控点。禁用点不默认显示在图上。

## 7. 开发优先顺序

Codex 执行任务时，请按以下顺序理解当前优先级：

1. 项目级输入值类型
2. 即时法（替换 Instant 占位页）
3. LJ + Z-score 建靶期离群值处理
4. 月度质控报告
5. 设置页（数据库位置 / 备份恢复 / 实验室信息 / 报告声明）

如用户任务与此顺序冲突，以用户明确任务为准。

## 8. 测试与验证

在完成代码修改后，优先运行仓库当前已有的检查方式：

```powershell
python -m py_compile app.py database.py zscore_logic.py zscore_plotting.py tests/zscore_smoke_test.py qc_logic.py plotting.py
python tests/zscore_smoke_test.py

如果任务涉及新模块，应补充最小可运行验证；如果改动影响页面流程，请至少进行对应路径的手工回归说明。

## 9. 提交要求

提交结果时请尽量说明：

改了哪些文件
为什么这样改
是否有数据库迁移
是否影响旧数据兼容
做了哪些验证
当前还留了什么后续事项

## 10. 禁止事项
不要重新引入“录入时实时切换/计算值类型”的主交互
不要把数据库位置迁移做成要求用户手输路径的流程
不要为了实现离群值处理而删除原始记录
不要把禁用点默认继续画在主图里
不要把“性能验证”带回 V1 范围